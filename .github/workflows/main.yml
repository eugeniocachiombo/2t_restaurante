name: Testing CI/CD                
on: [push]               
jobs:
  Deploy:                 
    runs-on: ubuntu-latest  
    steps:
      - uses: actions/checkout@v3  
      - name: Run tests            
        run: php artisan serve              